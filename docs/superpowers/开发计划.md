# 会议纪要智能体开发计划

## 开发总览

本计划包含从零开始搭建会议纪要智能体的所有步骤，按照三个阶段进行：

- **阶段一**：基础环境与项目框架（已完成 ✅）
- **阶段二**：核心功能开发（正在进行中）
- **阶段三**：增强功能与优化
---

## 完成状态

### 阶段一：基础环境与项目框架 ✅

#### 1. 开发环境搭建 ✓
- [x] JDK 21 安装与配置 (`ms-21.0.11`)
- [x] Maven 3.9.16 安装
- [x] Node.js v24.15.0 安装
- [x] Docker Desktop 4.78.0 安装
- [x] Docker Compose v5.1.4 安装
- [x] Git 配置

#### 2. 项目初始化 ✓
- [x] 创建项目结构（backend, frontend, docker, docs, prd）
- [x] 初始化 Git 仓库
- [x] 创建 README.md, .gitignore, CLAUDE.md
- [x] 创建 IntelliJ IDEA 项目配置

#### 3. Docker 服务部署 ✓
- [x] PostgreSQL 15 容器（含 pgvector 扩展）
- [x] Redis 7 缓存容器
- [x] FunASR 语音识别服务容器
- [x] Spring Boot 后端容器
- [x] React 前端容器
- [x] Nacos 2.3.0 配置中心容器
- [x] Docker Hub 镜像加速配置（`docker.1ms.run`）

#### 4. 数据库搭建 ✓（旧表，用于语音管线）
- [x] 创建 `meeting_minutes` 表（会议纪要）
- [x] 创建 `meeting_vectors` 表（向量存储，pgvector）
- [x] 创建 `dialogues` 表（对话记录）
- [x] 创建 `dialogue_messages` 表（对话消息）
- [x] 配置全文搜索 GIN 索引
- [x] 配置 Trigram 模糊搜索（pg_trgm）
- [x] 配置向量索引（ivfflat）

#### 5. 数据库 CRUD 验证 ✓
- [x] meeting_minutes 增删改查测试
- [x] meeting_vectors 增删改查测试
- [x] dialogues 增删改查测试
- [x] dialogue_messages 增删改查测试

#### 6. 后端基础框架 ✓
- [x] Spring Boot 项目搭建（Java 21）
- [x] 实体类定义（MeetingMinutes, MeetingVector, Dialogue, DialogueMessage）
- [x] Repository 层（JPA + 原生查询）
- [x] REST API 接口
  - [x] `POST /api/upload` - 文件上传
  - [x] `GET /api/meeting/{id}` - 获取会议详情
  - [x] `GET /api/meetings` - 获取会议列表
  - [x] `GET /api/health` - 健康检查
  - [x] `GET /api/search` - 智能搜索
  - [x] `POST /api/dialogue` - 创建对话
  - [x] `POST /api/dialogue/{id}/message` - 添加消息
  - [x] `GET /api/dialogue/{id}` - 获取对话详情
  - [x] `GET /api/dialogues` - 获取对话列表
  - [x] `POST /api/dialogue/{id}/archive` - 归档对话
  - [x] `POST /api/meeting/{id}/import` - 导入会议到对话

#### 7. 搜索功能（基础版本） ✓
- [x] PostgreSQL 全文搜索（`to_tsvector` / `plainto_tsquery`）
- [x] Trigram 模糊搜索（`pg_trgm` / `ILIKE` / `similarity()`）
- [x] 中文搜索支持
- [x] 搜索结果去重与排序

---

### 阶段二：核心功能开发（旧有功能）

#### 8. ~~RAG 向量搜索~~ ⚠️ 已重构为独立 RAG 管线

旧有的 RAG 实现（`VectorizationService` + `SearchService` 基于 `meeting_vectors` 表）已冻结，
新的 RAG 系统基于[独立设计文档](docs/superpowers/rag/rag-design.md)重新实现，使用新的表结构和算法。

**架构变更（2026-06-22）：**

| 维度 | 旧实现 | 新设计 |
|---|---|---|
| 表结构 | `meeting_minutes` + `meeting_vectors` | `document` + `document_chunk`（新表） |
| 会话 | `dialogues` + `dialogue_messages` | `conversation` + `message`（新表） |
| ORM | Spring Data JPA（保留） | 保留 JPA，不引入 MyBatis-Plus |
| Agent | AgentScope HarnessAgent（保留） | 保留 HarnessAgent，预检索注入 |
| 检索算法 | 简单合并去重 | RRF + 时间衰减 + MMR + 证据评估（工具类） |
| 配置中心 | Nacos（保留） | Nacos 管理公共配置，RAG 参数本地 yml |
| 语音识别 | FunASR（保留，独立管线） | 与 RAG 并行，互不依赖 |

**新 RAG 管线表（追加到 init-db.sql）：**
- `document` — RAG 文档/会议转写数据
- `document_chunk` — 文档分块 + 向量（1536 维）
- `conversation` — 新会话表
- `message` — 新消息表

> 旧表（meeting_minutes / meeting_vectors / dialogues / dialogue_messages）继续用于 FunASR 语音转写管线，
> 新表（document / document_chunk / conversation / message）用于 RAG 管线，两条管线通过 ChatService 统一呈现。

#### 8b. RAG 新管线实现（基于设计文档）
- [x] 新表 DDL（document, document_chunk, conversation, message → init-db.sql）
- [x] JPA Entity + Repository（DocumentEntity, DocumentChunkEntity, ConversationEntity, MessageEntity）
- [x] 检索算法工具类（RrfMerger, TimeDecayScorer, MmrDeduplicator, EvidenceEvaluator, CitationBuilder）
- [x] VectorSearchService + FullTextSearchService（基于 JPA Repository）
- [x] QueryPlanningService（DIRECT / REWRITE / DECOMPOSE）
- [x] HybridSearchService（编排检索算法工具类）
- [x] Document ETL Service（上传 → 切分 → 向量化 → 入库）
- [x] ChunkStrategy（结构化感知切分：发言人识别、总结/决策标记）
- [x] Document Controller（上传/列表/删除 API）
- [x] ConversationService + SummaryService（会话管理 + Token 感知滚动摘要）
- [x] ChatService 增强（ConversationChatController + HybridSearchService 预检索注入）
- [x] Conversation / Message / Feedback Controller
- [x] Nacos 配置同步（rag.* 参数保留在 application.yml）

#### 9. 语音转写功能
- [x] ~~FunASR 服务集成~~（已替换为 MiMo-V2.5-ASR）
- [x] MP4 音频提取（FFmpeg）
- [x] 异步转写处理
- [x] 转写结果存储（数据库只存元数据/索引，音频+文本存本地 upload 目录）
- [x] 进度状态跟踪
- [x] 移除自动向量化：转写 → 存储是独立步骤，向量化由用户主动触发
- [x] ~~对接 WebSocket 服务（`funasr-wss-server-2pass`）~~（已替换为云端 HTTP API）

#### 9b. 语音识别引擎替换：FunASR → MiMo-V2.5-ASR（2026-06-29）
- [x] 删除 FunASR WebSocket 客户端代码（TranscriptionService.callFunASR()）
- [x] 新增 MiMo-V2.5-ASR HTTP API 调用（OpenAI 兼容，音频 base64 传输）
- [x] 清理 docker-compose：移除 funasr-service 服务和依赖
- [x] 删除 docker/funasr/ 目录
- [x] 配置文件迁移：funasr.url → mimo.api-key + mimo.url

#### 10. 对话系统
- [x] AI 对话集成（DeepSeek V4 Flash 云端API）
- [x] 流式响应支持（SSE 流式输出）
- [x] 消息历史管理
- [x] ChatService 重构为 agentscope-harness（HarnessAgent）
- [x] 在线聊天 SSE 端点（`GET /api/chat/stream/{dialogueId}`）
- [x] 多模态支持（ZhiPu GLM-4V，图片识别）
- [x] 智能路由：有图片走 GLM-4V，纯文本走 DeepSeek

#### 11. Nacos 配置中心
- [x] Nacos 2.3.0 Docker 容器部署
- [x] 后端接入 Nacos 配置中心（`spring-cloud-starter-alibaba-nacos-config`）
- [x] `meeting-agent.yaml` 配置发布（deepseek、datasource、mimo）
- [x] API Key 等敏感配置迁移至 Nacos
- [x] 热更新支持（`@RefreshScope`）

#### 11. 前端重构
- [x] ChatGPT 风格布局重构
- [x] 侧边栏集成（对话列表/会议列表/搜索/上传）
- [x] 对话消息气泡 + Markdown 渲染
- [x] 流式打字机效果

---

### 阶段三：增强功能与优化

#### 11. RAG 意图识别（相关性阈值过滤）✅
- [x] 向量检索返回余弦相似度分数（`ScoredVector`）
- [x] 相关性阈值（0.6）：低于阈值的 RAG 结果不注入上下文
- [x] 双模式提示：有上下文时要求引用来源，无上下文时正常对话
- [x] 阈值生效验证：普通聊天/不相关问题不会触发 RAG

#### 12. 风格改写 ✅ (2026-06-24)
- [x] **数据库**：新建 `rewrite_result` 表（多文件源 + 版本化），`rewrite_feedback` 表（段落级反馈）
- [x] **意图识别**：ChatController 关键词匹配（改写/润色/rewrite），路由到 RewriteService
- [x] **风格学习**：StyleLearningService 向量检索加权排序（`similarity * (1 + 0.2 * priority_score)`）
- [x] **改写流程**：DeepSeek API SSE 流式调用 + `rewrite_result` 持久化 + 对照版 .docx 生成
- [x] **段落反馈**：后端按 `\n\n` 分段，前端每段 👍/👎 → priority_score ±0.5
- [x] **多文件支持**：`source_file_ids` 存 JSON 数组，一次改写可整合多个文件
- [x] **多次改写**：`version` 递增，"换一种风格"排除已用参考
- [x] **风格范例标记**：`meeting_minutes.style_exemplar` + `style_tags`，知识库页面星标手动标记
- [x] **风格标记前端**：知识库列表每项显示空心/实心星标图标，点击切换标记状态
- [x] **风格选取随机化**：`searchStyleExamples()` SQL 加 JOIN + `ORDER BY style_exemplar DESC`，Java 层打乱后取 top limit

#### 12b. Agent 驱动知识库上传 + 会议日期提取（2026-06-24 ~ 2026-06-25）
- [x] **回滚聊天自动知识库**：移除 chat upload 的自动向量化，用户反馈有误时撤回
- [x] **Tool Calling 实现**：`UploadToKnowledgeBaseTool` 实现 `AgentTool` 接口，agent 通过函数调用完成知识库上传，替换关键词匹配方案
- [x] **会议日期提取**：`MeetingDateExtractor` + `DeepSeekChatClient` LLM 提取文档/录音中的会议实际日期，保存到 `meeting_minutes.meeting_date`，知识库列表展示会议时间而非导入时间

#### 12c. 完整文档风格引用 + Word 表格输出（2026-06-25）
- [x] **完整文档引用**：`buildFullDocumentReferences()` 替代 chunk 片段方案，通过 `meetingRepository.findById()` 加载完整文档内容（优先读文件，fallback 转写字端），保留原始 tab 分隔表格格式
- [x] **用户勾选 + RAG 排序合并**：`manualReferenceIds`（用户勾选风格范例）置顶，RAG 向量检索补充排序结果，`LinkedHashSet` 去重
- [x] **Word 表格渲染**：`renderRichContent()` 逐行解析 LLM 输出，将含 tab 字符的连续行渲染为真实 Word 表格（`XWPFTable`），替代纯文本段落输出

#### 12d. 与会人收集 + 改写二次校验（2026-06-26 ~ 2026-06-27）
- [x] **与会人收集**：知识库导入文档时，`VectorizationService.vectorizeMeeting()` 调用 `DeepSeekChatClient` 提取文档中"与会人"栏目的名单，合并去重写入全局 `{uploadDir}/knowledge-base/与会人.md` 文件
- [x] **改写二次校验**：`RewriteService.streamRewrite()` 在改写流完成后，新增非流式校验步骤：① 读取 `与会人.md` 核对所有人名（如"张涛"→"张弢"） ② 核对部门/机构名称是否与原文一致（如"一营管理部"→"运营管理部"） ③ ICT 行业术语检查 ④ 错别字与语法修正
- [x] **SSE corrected 事件**：校验发现问题时，后端通过 `corrected` 事件推送修改后的完整内容
- [x] **竞态条件修复**：修复 `ChatService.streamChatWithDeepSeek()` 和 `streamChatWithZhipu()` 中 `emitter.complete()` 在 `dialogueService.addMessage()` 之前执行的 bug，确保消息先入库再通知前端

#### 12. 前端功能完善
- [x] 搜索界面优化（高亮、分页）
- [x] 对话界面完善
- [x] 转写任务进度展示
- [x] **文件上传增强**：每文件独立 loading 状态、图片缩略图预览、上传完成后通过回车将文件+消息发送给大模型
- [x] 多模态支持（ZhiPu GLM-4V v6）
- [x] **知识库页面**：浏览、搜索、查看已向量化的知识文档详情
- [x] **侧边栏导航**：对话/知识库视图切换（2026-06-22）

#### 13. 流式可靠性提升（2026-06-23）
- [x] **断连续传**：客户端断开 SSE 连接后，后端继续消费 LLM 流直到完整回复，存入 DB（`clientDisconnected` 标志位 + `finally` 块保存）
- [x] **刷新不丢响应**：页面刷新后重新加载对话，自动获取已保存的完整 LLM 回复，不再看到截断内容

#### 14. 文件预览与持久化（2026-06-23）
- [x] **输入区文件卡片整合**：上传后文件以卡片形式显示在输入框上方，支持删除和快速指令（详细总结/简单摘要/提取要点）
- [x] **文件附件刷新持久化**：`dialogue_messages.metadata` TEXT 列存储 `{"fileIds":[1,2,3]}`，刷新后通过 `loadFiles → loadMessages` 链恢复文件附件
- [x] **文件预览抽屉**：点击消息气泡中带下划线的文件名，右侧 Drawer 展示文件内容（文本渲染、图片展示、空状态）
- [x] **文本内容端点**：`GET /api/meeting/{id}/text-content` 专用接口，返回文件文本内容供预览
- [x] **文档格式扩展**：支持 .doc/.docx（Apache POI）、.pdf（Apache PDFBox）文本提取预览，覆盖 txt/md/csv/json/xml/html/yaml/properties/log 等格式

#### 15. 视频上传转写能力展示 ⏸️ 暂缓
- [ ] 前端上传页面/组件展示：视频上传 → 转写进度 → 纪要展示 → 搜索演示（暂缓，先聚焦核心功能打磨）

#### 16. 概要记忆系统（Agent 通用能力 — 记忆化存储）✅
- [x] 后端 ProfileService + ProfileController（profile .md 文件 CRUD）
- [x] ChatService 注入 profile 内容到 system prompt（通过 user message 注入）
- [x] 注册 `read_profile` / `update_profile` Agent 工具
- [x] 迁移与会人.md 路径（knowledge-base → profile）
- [x] 前端 ProfileSettings 组件（多标签 markdown 编辑）
- [x] 启动迁移逻辑（旧路径→新路径，自动复制）
- [x] 前端侧边栏新增"个人设置"入口

#### 16b. 对话消息持久化改造 — PgAgentStateStore（2026-07-01）✅
- [x] 新建 agent_sessions 表替代旧 dialogues + dialogue_messages 表
- [x] PgAgentStateStore 实现 AgentStateStore 接口，JSONB 全量快照持久化
- [x] SessionEntity + SessionRepository + SessionService + SessionController
- [x] ChatService 去除 7 处 manual dialogueService.addMessage() 调用
- [x] HarnessAgent builder 添加 .stateStore(pgAgentStateStore)，启用自动持久化
- [x] 自检修正：直接通过 PgAgentStateStore 更新 AgentState
- [x] RewriteService 替换 DialogueService 为 SessionService
- [x] 移除 SearchController/SearchAgent 中的 dialogueService 调用
- [x] 删除旧文件：Dialogue.java, DialogueMessage.java, DialogueRepository, DialogueMessageRepository, DialogueService, DialogueController, DialogueAgent, DialogueServiceTest
- [x] init-db.sql 更新为 agent_sessions 为主
- [x] FK 迁移：meeting_minutes.dialogue_id → agent_sessions, rewrite_result.dialogue_id → agent_sessions
- [x] 旧数据迁移脚本（dialogues → agent_sessions + AgentState JSON 构建）
- [x] 新增 PgAgentStateStoreTest（13 个测试）+ SessionServiceTest（16 个测试）
- [x] 移除 VectorizationServiceTest 中 2 个失效测试

#### 16c. 文件上下文修复 + 知识库误调用修复（2026-07-01）✅
- [x] **文件上下文区分**：`buildFileBlocks` 仅返回图片 block，文本内容剥离到 `buildFileTextContext` 按"本次提交的文件"（高优先级）和"对话历史中的文件"（低优先级）分区间隔，避免新文件回答被旧文件干扰
- [x] **知识库误调用限制**：`ChatService` 系统提示词新增"关于 upload_to_knowledge_base 的严格规则"段落，明确禁止在"总结/摘要/要点/改写/分析/查看"时调用；`UploadToKnowledgeBaseTool` 描述同步加强限制，修复双引号编译问题
- [x] **快速指令自动发送**：输入区文件卡片上方的"详细总结/简单摘要/提取要点"悬浮按钮改为直接调用 `sendMessage()` 自动发送消息，无需手动回车

#### 16c. 深色/浅色主题切换（2026-07-01）✅
- [x] CSS 变量体系（`--app-bg`、`--sider-bg`、`--text-color` 等），`[data-theme="dark"]` 下切换深色值
- [x] `ConfigProvider` + `theme.darkAlgorithm` / `theme.defaultAlgorithm` 自动切换 Ant Design 组件
- [x] 侧边栏底部切换按钮（`SunOutlined` / `MoonOutlined`），localStorage 持久化偏好
- [x] `App.tsx` / `DialoguePanel.tsx` 中所有硬编码颜色替换为 CSS 变量引用

#### 17. 性能优化
- [ ] Redis 缓存集成
- [ ] 数据库查询优化
- [ ] 大文件处理优化

#### 18. 文档与测试
- [ ] API 文档
- [ ] 单元测试
- [ ] 集成测试
- [ ] 部署文档

---

### 阶段四：Agent 能力增强（2026-06-29 规划）

#### 19. Agent 工具扩展（搜索/列表/联网）
- [x] SearchKnowledgeBaseTool — 语义搜索知识库（包装 VectorizationService.searchSimilarWithScores）
- [x] ListMeetingsTool — 会议列表分页浏览
- [x] SearchMeetingTitlesTool — 标题模糊搜索
- [x] Tavily MCP 集成 — `npx tavily-mcp` Stdio 子进程
- [x] ChatService 改造：删除硬编码 buildRagContext()，System Prompt 扩展
- [x] Docker 镜像安装 Node.js（for tavily-mcp）

#### 20. Agent 推理过程可视化（Thinking Chain）
- [x] 后端：ChatService 添加 THINKING_BLOCK_DELTA / TOOL_CALL_START / TOOL_RESULT_START 等事件转发为 SSE
- [x] 后端 SSE 事件：`event: thinking`（思考过程）、`event: tool_call`（工具调用）、`event: tool_result`（工具结果）
- [x] 前端：ThinkingBlock 组件（灰色斜体气泡 + 省略号动画）
- [x] 前端：ToolCallBlock 组件（工具调用卡片 + 结果折叠展示）
- [x] 前端：整合打字机效果，thinking/tool_call 与 text delta 混合渲染

#### 21. 自我反思（Self-reflection）
- [x] 聊天自检：回复完成后触发 DeepSeek 非流式检查，SSE `corrected` 事件推送修正版
- [x] 触发条件：仅在 agent 调用了工具时执行（invokedTools 标志位）
- [x] 转录自检：TranscriptionService 中 MiMo ASR 返回后追加 DeepSeek 校验
- [x] 自检 prompt：重点检查人名/术语/数字，修正后覆盖更新 transcription

#### 22. 与会人搜索修复 — Query Rewriting + 专用向量块（2026-07-01）✅
- [x] MeetingMinutes 增加 `participants` TEXT 字段，存储提取的与会人名单
- [x] QueryRewriter Service（LLM 提取搜索关键词，Fallback 规则分词）
- [x] VectorizationService 正则提取"与会人"段落，创建 `chunk_index=-1` 专用向量块
- [x] `collectParticipants()` LLM 提取的与会人同时写入 DB participants 字段
- [x] SearchKnowledgeBaseTool 集成 QueryRewriter：原句向量搜索 + 关键词 ILIKE 精确匹配
- [x] 更新 fullTextSearch / trigramSearch SQL 覆盖 participants 列
- [x] `POST /api/meetings/backfill-participants` 存量数据补全接口

#### 23. 通用子 Agent 动态委派（2026-07-01）✅
- [x] 发现 AgentScope 内置 `general-purpose` 通用子 agent（HarnessAgentBuilderSupport 自动注册）
- [x] ChatService SYSTEM_PROMPT 添加子 agent 使用指引：`general-purpose` 用于任意独立任务，`rewrite_agent` 用于会议纪要改写
- [x] HarnessAgent builder 添加 `.description("会议纪要智能助手，支持子 agent 委派")`
- [x] 主 agent 可随时调用 `agentSpawn("general-purpose", task="任意任务", timeout)` 动态委派

#### 25. 全面工具化 — MiMo ASR / ZhiPu 图片理解（2026-07-03）✅
- [x] 新建 `CallMiMoAsrTool`：MiMo-V2.5-ASR 音频/视频转写工具，实现 `AgentTool` 接口
- [x] 新建 `UnderstandImageTool`：ZhiPu GLM-4V 图片理解工具，实现 `AgentTool` 接口
- [x] AgentConfig Toolkit 注册 2 个新工具
- [x] SYSTEM_PROMPT / AGENTS.md 更新工具说明和子 agent 使用规则
- [x] TranscriptionService 简化：删除校对逻辑，只保留 ASR 转写
- [x] ChatService 移除 ZhiPu 硬编码分支（streamChatWithZhipu / buildFileBlocks）
- [x] 创建 .agentscope/workspace/AGENTS.md 主 agent 人格定义
- [x] 创建 subagents/response-checker.md（回答质量校验）
- [x] 创建 subagents/transcription-checker.md（转写文本校对）

#### 26. ChatService 架构重构 — 生产稳定性优化（2026-07-03）✅
- [x] **CleanablePgAgentStateStore**：继承 PgAgentStateStore，save() 时自动清洗 enriched UserMessage，消除 cleanUpUserMessageInState 的读-改-写竞态条件
- [x] **ChatService 拆分为 4 个组件**：
  - `FileContextBuilder` — 文件上下文构建，含 10K 总长度上限
  - `DialoguePersistenceService` — 对话消息持久化（事务管理）
  - `ChatStreamService` — SSE 流控制、事件分发
  - `ChatService` — 精简为 Facade（406→90 行）
- [x] **SseEventTypes**：SSE 事件枚举 + 数据 record，消除魔法字符串
- [x] 删除冗余 `setPermissionMode`（构建时已设置 BYPASS）
- [x] PgAgentStateStore 移除 @Component，由 CleanablePgAgentStateStore 接管
- [x] 清理死代码：SearchAgent / TranscriptionAgent / FileProcessingAgent / SearchGuard
- [x] .gitignore 更新：追踪 .agentscope/workspace 配置文件
- [x] .idea/meeting-agent.iml 移除 excludeFolder，IDEA 不再橙色显示
- [x] PgAgentStateStoreTest 新增 4 个 enriched 清洗测试用例

---

### 阶段四：Agent 能力增强（2026-06-29 规划）
- [x] 差距分析：双管线未打通、缺乏修正性RAG、缺少LLM重排序、缺少Agent多步检索规划
- [x] 新建 SearchDocumentsTool 包裹 HybridSearchService，暴露 RRF/证据评估/引文能力给 Agent
- [x] ChatService 注册 search_documents 工具 + 更新系统提示词（工具说明、evidenceLevel 解读、修正性RAG引导、多步检索规划）
- [x] 实现 DeepSeekReranker（LLM 重排序），通过 `rag.retrieval.rerank-enabled=true` 配置开关控制
- [x] Agent 证据感知重试：evidenceLevel 为 WEAK/NONE 时自主改写查询重试
- [x] Agent 多步检索规划：支持复杂查询分步检索（先搜后分析再搜）

---

## 开发时间估算

| 阶段 | 任务 | 预估时间 | 状态 |
|------|------|----------|------|
| 一 | 基础环境与项目框架 | 2 天 | ✅ 已完成 |
| 二 | ~~向量搜索（旧）~~ | — | ⚠️ 已冻结，被新 RAG 管线替代 |
| 二 | **RAG 新管线实现** | **3-5 天** | 🔄 新任务 |
| 二 | 语音转写集成 | 1-2 天 | ⏳ 待开始（与 RAG 并行） |
| 二 | 对话系统 + 前端重构 | 2-3 天 | ✅ 已完成 |
| — | Nacos 配置中心 | — | ✅ 已完成 |
| 三 | RAG 增强（Rerank / 自适应调参 / Redis 缓存） | 2-3 天 | - |
| 三 | 风格改写（意图识别 + 风格学习 + 段落反馈） | 1 天 | ✅ 已完成 |
| 三 | 与会人收集 + 改写二次校验 | 0.5 天 | ✅ 已完成 |
| 三 | 流式可靠性 + 文件预览/持久化 | 1 天 | ✅ 已完成 |
| 三 | 前端完善 | 2-3 天 | ✅ 已完成 |
| 三 | 视频上传转写能力展示 | — | ⏸️ 暂缓 |
| 三 | **概要记忆系统（Profile Memory）** | **0.5 天** | ✅ 已完成 |
| 三 | 测试与文档 | 1-2 天 | - |
| 三 | **语音引擎替换（FunASR→MiMo）** | **0.5 天** | ✅ 已完成 |
| 四 | **Agent 工具扩展（4 个工具）** | **1 天** | ✅ 已完成 |
| 四 | **推理过程可视化（Thinking Chain）** | **1 天** | ✅ 已完成 |
| 四 | **自我反思（聊天 + 转录自检）** | **0.5 天** | ✅ 已完成 |
| 四 | **全面工具化（MiMo ASR + ZhiPu）** | **0.5 天** | ✅ 已完成 |
| 四 | **ChatService 架构重构（拆分 + 竞态修复）** | **0.5 天** | ✅ 已完成 |
| 四 | **SSE 流式架构升级（SseEmitter → WebFlux）** | **0.5 天** | ✅ 已完成 |

---

---
	
### 25. 消息表拆分 — 从 state_json 到独立 dialogue_messages（2026-07-03）✅
- [x] 设计决策：拆分写入放大问题，messages 行追加 vs state_json 全量反序列化
- [x] DialogueMessageEntity + DialogueMessageRepository（新增 JPA 实体和仓库）
- [x] SessionService 重构：`getSessionWithMessages` 查新表，`extractFilesFromState`/`findFilePathInState` 转查新表，`addMessage` 双写
- [x] ChatService 流结束后双写：`state_json`（Agent 上下文）+ `dialogue_messages`（user+assistant 两行）
- [x] UploadToKnowledgeBaseTool 改查新表（`extractLastUserMessage`）
- [x] 前端简化：`DialogueMessage` 接口加 `files` 字段，`loadMessages` 不再解析 metadata 匹配文件
- [x] 测试适配：41 个测试全部通过
- [x] 清理旧表（dev 阶段全量数据清空）

#### 27. 架构升级 — conversation 包 CR 修复（2026-07-03）✅
- [x] DI 清理：全局 `@RequiredArgsConstructor` + `@ConfigurationProperties` 提取（FileProperties, MimoProperties, EmbeddingProperties）
- [x] JPA 关系映射：消除裸外键 Long → `@ManyToOne`/`@OneToMany(cascade=ALL, orphanRemoval=true)`
- [x] `@ElementCollection` 规范化：`sourceFileIds`/`referenceIds` 从 VARCHAR JSON → 独立 join 表
- [x] `AttributeConverter` 自动序列化：`files`/`metadata` 字段 TEXT ↔ Java 类型自动转换（JsonListConverter, JsonMapConverter）
- [x] 移除冗余 `messageCount` 字段（Lost Update 风险）
- [x] 外键列添加数据库索引（idx_dm_session_id, idx_rr_session_id, idx_rf_rewrite_result_id）

#### 28. 架构升级 — document 包 CR 修复（2026-07-04）✅
- [x] DocumentUploadService 硬编码路径 → 注入 `FileProperties`，统一配置管理
- [x] DocumentParserService 集成 `DocumentTextExtractor`：支持 PDF/DOCX/DOC 解析（原 fallback `Files.readString` 会读取二进制乱码）
- [x] Controller 编排逻辑提取到 Service：`processUpload()` 编排 upload → parse → chunk，Controller 只调用 Service
- [x] not-found 处理统一：`ApiResponse.error()` 返回 200 → `BusinessException.notFound()` 返回 404
- [x] DocumentChunkEntity 裸外键 → `@ManyToOne(fetch=LAZY)` + `@OneToMany(cascade=ALL, orphanRemoval=true)` 级联删除
- [x] 删除 `JsonbConverter`（`@Converter(autoApply=true)` 错误地应用于所有 String 列）
- [x] RagProperties 去掉 `@Component`，改用 `@EnableConfigurationProperties` 注册
- [x] ChunkService 移除 `ObjectMapper` 注入，改用 `JsonUtil.toJson()`

#### 29. 架构升级 — meeting 包 CR 修复（2026-07-04）✅
- [x] MeetingController 移除 Repository 直接注入，查询逻辑委托给 MeetingService
- [x] getMeeting() 返回值包装为 `ApiResponse<MeetingDetailVO>`
- [x] listMeetings() 返回 `ApiResponse<Page<MeetingMinutes>>` + 分页参数
- [x] deleteMeeting() 存在性检查移入 Service 层
- [x] VectorizationService + KnowledgeBaseService: `IllegalArgumentException` → `BusinessException`（500 → 404/400）
- [x] MeetingMinutesRepository: 参数绑定统一为 `@Param`，fullTextSearch/trigramSearch 添加 `LIMIT 50`
- [x] MeetingVector.priorityScore 添加 `nullable = false`
- [x] MeetingController 移除未使用的 `@Slf4j`

#### 30. meeting 包外部 CR 报告修复（2026-07-04）✅
- [x] searchByParticipants SQL 添加 `LIMIT 50`（防 OOM）
- [x] MeetingVectorRepository.deleteByMeetingId 移除 `@Transactional`（保留 `@Modifying`，调用方已有事务）
- [x] MeetingVector.priorityScore 添加 `columnDefinition = "double precision default 0.0"`（DB 层默认值）
- [x] MeetingMinutes.styleExemplar 添加 `columnDefinition = "boolean default false"`（DB 层默认值）

#### 31. retrieval 包 CR 修复（2026-07-04）✅
- [x] HybridSearchService 时间衰减 N+1 查询 → `documentRepository.findAllById()` 批量预取
- [x] HybridSearchService 重试逻辑修复：retry 结果走完整管线（RRF → 时间衰减 → 去重），分数口径一致
- [x] HybridSearchService `retried` 标志位移入 `if (retryTopScore > topScore)` 块内
- [x] HybridSearchService DECOMPOSE 支持：遍历 `plan.subQueries()` 执行多次搜索合并
- [x] HybridSearchService 提取 `executeSearch()` / `applyTimeDecay()` 私有方法
- [x] HybridSearchService.SearchResult.citations 类型 `List<Map>` → `List<Citation>`
- [x] QueryPlanningService JSON 解析：手写字符串查找 → Jackson ObjectMapper
- [x] QueryPlanningService DECOMPOSE 实现：解析 `sub_queries` 数组
- [x] MmrDeduplicator 重命名为 DocumentDeduplicator（实际逻辑是文档去重，非 MMR 算法）
- [x] VectorSearchService `ConcurrentHashMap` → `HashMap`（单线程方法无需并发容器）
- [x] CitationBuilder 返回类型 `List<Map>` → `List<Citation>`，Citation 添加 `@JsonProperty` 注解
- [x] DeepSeekReranker 正则修复：行首锚定 + 捕获冒号后分数（原正则捕获的是序号而非分数）
- [x] DeepSeekReranker Prompt 强化：明确要求只输出评分行，禁止解释性文字
- [x] FullTextSearchService topK 上限校验（`Math.max(1, Math.min(topK, 100))`）
- [x] ChunkResult vectorRank/ftsRank `int` → `Integer`（区分"未参与"和"排名为0"）
- [x] RrfMerger 分数修复：仅对实际命中的通路累加 RRF 分数（null rank 不贡献分数）
- [x] Citation 添加 `@NoArgsConstructor`（Jackson 反序列化兼容）
- [x] 算法类静态方法 → Spring Bean（RrfMerger/TimeDecayScorer/DocumentDeduplicator/EvidenceEvaluator/CitationBuilder 加 `@Component`，HybridSearchService 注入实例）

#### 32. retrieval 包 CR 修复 — 第三轮（2026-07-05）✅
- [x] HybridSearchService DECOMPOSE 子查询并行化：先收集所有 future，再统一 join
- [x] VectorSearchService N+1 修复：`findAllById()` 批量预取 DocumentEntity
- [x] DeepSeekReranker 解析失败默认分数 5 → 0（避免不相关文档获得中等排名）
- [x] HybridSearchService baseScore 统一使用 `rrfScore`（RRF 已融合 vector+fts 信息）

#### 33. state 包 CR 修复（2026-07-05）✅
- [x] CleanablePgAgentStateStore.cleanEnrichedMessages 深拷贝 context，save 不污染原始 AgentState
- [x] PgAgentStateStore.save() 非 AgentState 类型时 log.warn + return，不用空字符串覆盖数据
- [x] CleanablePgAgentStateStore metadata null 检查（防 NPE）
- [x] PgAgentStateStore 改为 abstract，防止直接实例化导致事务失效
- [x] PgAgentStateStore.get() 反序列化失败日志从 warn → error，记录完整异常栈
- [x] 新建 EnrichedMessageConstants 共享常量，FileContextBuilder + CleanablePgAgentStateStore 引用同一常量
- [x] 修复 PgAgentStateStoreTest 中已废弃的 messageCount 引用

#### 34. SSE 流式架构升级 — SseEmitter → WebFlux（2026-07-05）✅
- [x] ChatStreamService 重写：返回 `Flux<ServerSentEvent<String>>`，去掉 SseEmitter/blockLast/EventHandler
- [x] ChatService 签名改造：返回 Flux，去掉 TaskExecutor 和 SseEmitter 参数
- [x] ChatController 简化：直接 return Flux，去掉 emitter 创建和生命周期回调
- [x] CancellationToken 标记 @Deprecated（主流程不再使用）
- [x] 取消机制简化：SseEmitter 3 个回调 + CancellationToken → 1 行 doOnCancel
- [x] 持久化调度：阻塞 JPA 操作通过 Schedulers.boundedElastic() 隔离

#### 35. 刷新不丢对话 — 用户消息即时持久化 + 生成中状态检测（2026-07-05）✅
- [x] DialoguePersistenceService 拆分：`persistUserMessage()`（流开始前调用）+ `persistAssistantResponse()`（流结束后调用）
- [x] ChatStreamService 在 stream() 开始时立即持久化用户消息，handleComplete/handleError 仅持久化助手回复
- [x] 前端 DialoguePanel 检测"最后一条是 user 消息"→ 进入 generating 状态，3 秒轮询等待 assistant 消息出现
- [x] 生成中 UI：Spin + "AI 正在生成中..." 气泡，assistant 消息到达后自动消失
- [x] emitter.onTimeout 资源泄漏修复：completeWithError(TimoutException)

#### 36. 意图识别重构 — 删除 IntentRouter + IntentMiddleware，Agent 自主判断（2026-07-07）✅
- [x] 删除 IntentRouter.java：移除 LLM 调用、IntentResult record、所有死方法
- [x] ChatService 精简：移除 IntentRouter 依赖、handleClarification()、DialoguePersistenceService 依赖
- [x] ~~IntentMiddleware~~ 最终删除：Agent 自主判断意图，无需中间件注入提示
- [x] AgentConfig 清理：移除 IntentMiddleware 引用，仅保留 FileContextMiddleware
- [x] AGENTS.md 移除 intent_agent 指令，新增澄清行为指引，Agent 通过工具和子 agent 说明自行判断意图
- [x] 删除 intent_agent.md 子 agent 配置
- [x] rewrite_agent 从 AgentConfig 内联代码移到 subagents/rewrite_agent.md 文件声明
- [x] AGENTS.md 优化：新增典型场景工作流、区分搜索工具、强化追问行为、补充子 agent 使用规范

#### 37. RAG 管线统一 — meeting_minutes → document 迁移（2026-07-08）✅
- [x] DocumentEntity 新增 6 字段：transcription, styleExemplar, styleTags, participants, duration, mdFilePath
- [x] DocumentController 新增 4 个端点：text-content, file download, style-exemplar, style-exemplars
- [x] KnowledgeBaseService 改用 DocumentUploadService + DocumentRepository
- [x] Agent 工具迁移：ListMeetingsTool, SearchMeetingTitlesTool, UploadToKnowledgeBaseTool 改用新管线
- [x] 删除 SearchKnowledgeBaseTool（功能由 SearchDocumentsTool 替代）
- [x] StyleLearningService 改用 EmbeddingService + DocumentChunkRepository（styleExemplarSearch）
- [x] FileController 删除 /meeting/{id}/file 和 /meeting/{id}/text-content 端点
- [x] 删除 MeetingController 中旧端点，保留 /health 和 /dialogue/{id}/meetings
- [x] 前端 KnowledgeBase.tsx 切换到 Document API
- [x] 前端 api.ts 删除旧 Meeting API，扩展 RagDocument 接口
- [x] 前端 DialoguePanel.tsx, SearchPanel.tsx, MeetingList.tsx 适配新 API
- [x] 删除旧管线代码：MeetingMinutes, MeetingVector, MeetingService, VectorizationService, QueryRewriter, MeetingDetailVO, MeetingListVO, KBUploadVO
- [x] 创建 docker/migrate-rag.sql 数据迁移脚本
- [x] 更新 docker/init-db.sql：删除旧表定义，document 表新增字段
- [x] 更新 AGENTS.md：移除 search_knowledge_base 工具描述

#### 38. 文档清洗 — DOCX/PDF/DOC → Markdown 规则提取（2026-07-08）✅
- [x] DocumentTextExtractor 重写：DOCX 使用 XWPFDocument 遍历 body elements，转换段落（标题样式/编号/粗体斜体）和表格
- [x] DOCX 表格类型检测：元数据表格→会议信息表，内容表格→议题+发言人格式，通用表格→markdown 管道表
- [x] DOCX 自定义样式支持：Cap_Header, Cap_Heading_1~4 等非标准样式名映射到 markdown 标题层级
- [x] PDF 字体启发式：FontSizeAnalyzer 统计正文字号，MarkdownPdfStripper 按字号比判断标题层级
- [x] DOC 样式检测：HWPFDocument styleIndex 映射到标题层级
- [x] DocumentUploadService.processDocumentAsync() 保存清洗后的 .cleaned.md 文件，更新 transcription 和 mdFilePath
- [x] 创建 profile/会议纪要模板.md — 改写目标格式参考

#### 39. Chunk 策略升级 — Markdown 结构感知（2026-07-08）✅
- [x] StructuralChunkStrategy 新增 markdown 标题检测（`## 会议决策` 等 → 分块边界 + sectionType 映射）
- [x] 新增粗体发言人检测（`**刘赞作了相关汇报：**` → speaker 提取 + 分块边界）
- [x] 新增 markdown 表格原子性（表格行不从中间截断，整个表格作为一个 chunk）
- [x] findSentenceBoundary 增加 `#` 标题行检测，优先在标题前分割
- [x] 保留原有 SPEAKER_PATTERN 和 SECTION_PATTERN 作为 fallback
- [x] 修复：SECTION_KEYWORDS 过滤器防止 `会议讨论：` 等章节标题被误识别为发言人
- [x] 修复：size overflow 改为 while 循环，确保超长文本被多次分割至 size 以内
- [x] 修复：分割时添加 `[议题标题] [发言人]` 上下文前缀，保留语境

#### 40. 文件预览 — DOC/PDF/DOCX 原文件前端预览（2026-07-09）✅
- [x] 新建 PreviewDrawer 通用组件：PDF iframe 渲染、DOCX mammoth.js 转 HTML、图片直显、文本 fallback
- [x] api.ts 新增 getFileBlob(url) 方法
- [x] KnowledgeBase 改造：handlePreview 复用 PreviewDrawer
- [x] DialoguePanel 改造：handleFilePreview 复用 PreviewDrawer

#### 41. Speaker 解析增强 — 段内发言人识别（2026-07-10）✅
- [x] SPEAKER_PATTERN 移除 `^` 行首锚定，改用 `(?<=^|[^\\u4e00-\\u9fff])` 负向后顾（CJK 字符边界约束）
- [x] `parseDiscussionContent()` 重写：`Matcher.find()` 循环遍历全文而非逐行分割，支持表内合并文本的多发言人检测
- [x] 测试验证：9/9 发言人正确识别（Topic 1: 刘赞→张力→张弢→于总；Topic 2: 曾富贵→罗家承→刘赞→张弢→于总），段落边界严格遵守中文标点

#### 42. Chunk 策略升级 — h3 议题为主 + speaker 级联切割（2026-07-10）✅
- [x] h3 议题作为主 chunk 边界，议题内所有 speaker 内容优先积累为同一 chunk
- [x] 超长时按 speaker 边界级联切割，每个子 chunk 带 h3 标题上下文
- [x] 单个 speaker 段落仍然超长时，回退到句子边界（。！？）切割，带 h3 标题 + speaker 上下文
- [x] 容器标题过滤：`## 会议讨论` 等无实质内容的标题不产生空 chunk
- [x] 浮动阈值：触发线从 `config.getSize()`（512）提升到 `config.getSize() + 100`（612），允许 ±100 字符浮动
- [x] 兼容性：纯 h2 文档行为不变，不影响已有测试

*开发计划创建日期：2026-06-19*
*最后更新：2026-07-13 (时间意图识别)

#### 43. Chunk 优化 — 元数据分离 + 内容清洗（2026-07-10）✅
- [x] DocumentTextExtractor 新增 MeetingInfo record + extractMeetingInfo() 从 markdown 提取会信息息
- [x] DocumentUploadService 将 meetingDate/participants/duration 写入 DocumentEntity
- [x] ChunkSegment 新增 topic/sectionHeading/documentTitle 字段
- [x] StructuralChunkStrategy 跳过 `## 会议信息` 章节（不 chunk）
- [x] StructuralChunkStrategy 追踪 h2/h3 章节元数据，写入 ChunkSegment
- [x] ChunkService 剥离 `[overlap]` 标记，构建丰富 metadata JSON（document_title/meeting_date/participants/topic/section_heading）

#### 44. Chunk 通用化 — 预处理 + GenericChunkStrategy（2026-07-10）✅
- [x] 新增 MarkdownPreprocessor：剥离标题行和 `## 会议信息` 章节，所有文档格式定制集中于此
- [x] 新增 GenericChunkStrategy：通用 markdown 分块（h2/h3 边界 + size 溢出 + 句子边界），不含会议格式知识
- [x] ChunkService 集成预处理，分块前调用 markdownPreprocessor.preprocess(text)
- [x] GenericChunkStrategy 为默认策略（@ConditionalOnProperty matchIfMissing=true）
- [x] 新增 GenericChunkStrategyTest（8 个测试覆盖章节边界、h3 议题、长内容溢出、表格原子性、非会议文档、空内容）

#### 45. FullTextSearchService 分层重构 — 抽取 Repository 层（2026-07-12）✅
- [x] 新增 retrieval/repository/FullTextSearchRepository.java：抽取 JdbcTemplate SQL + RowMapper
- [x] FullTextSearchService 精简：去除 JdbcTemplate/ObjectMapper，注入 FullTextSearchRepository
- [x] 新增 FullTextSearchService 空查询守卫（null/blank → 空结果）
- [x] 新增 retrieval/model/ChunkMetadataParser.java：metadata JSON 解析工具类
- [x] VectorSearchService 同步重构：替换内联 ObjectMapper 解析为 ChunkMetadataParser

#### 46. PostgreSQL 中文分词升级 — zhparser（2026-07-13）✅
- [x] Dockerfile 新增 SCWS 1.2.3 + zhparser 编译安装
- [x] init-db.sql 创建 chinese 分词配置，content_tsv 改为 to_tsvector('chinese', ...)
- [x] 新建 docker/migrate-zhparser.sql 存量数据库迁移脚本
- [x] FullTextSearchRepository 查询配置改为 plainto_tsquery('chinese', ?)

#### 47. BGE Reranker 本地推理 — ONNX Runtime（2026-07-13）✅
- [x] pom.xml 添加 onnx-runtime + DJL tokenizer 依赖
- [x] 新增 BgeReranker 实现 Reranker 接口，ONNX Runtime 进程内推理
- [x] 删除 DeepSeekReranker.java（由 BGE 替代）
- [x] NoOpReranker/BgeReranker 按 rerank-enabled 配置互斥
- [x] docker-compose.yml 新增模型目录挂载
- [x] 新建 scripts/download-bge-reranker.sh 模型导出脚本

#### 48. 时间意图识别 — 检索管线时间感知（2026-07-13）✅
- [x] QueryPlan 新增 TimeIntent 枚举（RECENT/HISTORICAL/NEUTRAL/RANGE）+ timeRange 字段
- [x] QueryPlanningService prompt 增加时间意图分析维度，解析 LLM 输出
- [x] HybridSearchService 根据 TimeIntent 条件执行时间衰减：RECENT 走现有逻辑，HISTORICAL/NEUTRAL/RANGE 跳过衰减保留语义排序

#### 49. RAG 分数语义统一 [-1, 1] + 启用 BGE Reranker（2026-07-13）✅
- [x] BgeReranker softmax 概率 → 2*prob-1 映射到 [-1, 1] 语义区间
- [x] RRF 分数归一化：rrfScore / maxRrf (2/(k+1)) → [0,1] → 2*x-1 → [-1, 1]
- [x] applyDecayWithIntent 统一走 score01ForChunk + [-1, 1] 映射，Reranker 和 RRF 路径一致
- [x] 修复 Reranker 分数被 applyDecayWithIntent 覆盖的 bug
- [x] 移除 TimeDecayScorer 依赖，内联到 applyTimeDecay
- [x] 下载并导出 BAAI/bge-reranker-v2-m3 ONNX 模型 (~2.1 GB)
- [x] application.yml rerank-enabled: true
- [x] application-test.yml rerank-enabled: false（测试环境不加载模型）

#### 50. search_agent 子 Agent — Agentic RAG 改造（2026-07-13）✅
- [x] HybridSearchService 移除自动重试（Step 5 + generateRetryQuery + retried）
- [x] SearchDocumentsTool 移除 retried 引用
- [x] AgentConfig 注册 search_agent 子 Agent（SubagentDeclaration + 系统提示词）
- [x] AGENTS.md 更新主 Agent 行为指引，检索委托 search_agent

#### 51. RAG 管线架构清理 + metadata 增强（2026-07-13）✅
- [x] RRF 分数归一化到 [-1, 1]（score01ForChunk 统一 Reranker/RRF 路径）
- [x] expandNeighbors 改为条件执行（evidence >= PARTIAL 才扩）
- [x] expandNeighbors 补全 fileName/meetingDate/participants/topic/sectionHeading
- [x] ChunkMetadataParser 增加 participants/topic/section_heading 解析
- [x] ChunkResult 增加 participants/topic/sectionHeading 字段
- [x] SearchDocumentsTool 透传 metadata 给 Agent
- [x] 删除 SearchController + SearchResultVO（前端已无调用方）
- [x] 删除前端 DialoguePanel 搜索弹窗（setSearchVisible(true) 从未被调用）
- [x] 删除 api.ts searchDocuments 函数
- [x] SearchPanel 改为文档标题搜索 GET /api/document/search
- [x] DocumentController 新增 @GetMapping("/search") 标题模糊搜索
- [x] 修复 expandNeighbors 中 ObjectMapper 注入

#### 52. RAG 检索质量评估系统（2026-07-13）✅
- [x] DDL：eval_test_case / eval_run / eval_result 三张表
- [x] 初始测试集 10 条（匹配实际文档）
- [x] Python 评估脚本（NDCG@3 / P@3 / R@3 / MRR / latency）
- [x] 运行基线评估（P@3=0.33, MRR=1.0, E@top=0%）

#### 53. RAG 评估系统二期 — Java EvalRunner + RAGAS 指标（2026-07-14）✅
- [x] 弃用 Python 评估脚本，改为纯 Java 实现（EvalRunner，IDEA 右键 Run）
- [x] 测试集从 DB 迁到 JSON 文件（scripts/eval/test_cases.json）
- [x] EvalRunner 支持基础指标（NDCG / P@3 / R@3 / MRR / latency / E@top）
- [x] RAGAS 指标集成（--ragas 开关，直接调 DeepSeek API，不需要 Python）
- [x] 对比分析（--compare，逐项 diff + 切片对比 + 聚合汇总）
- [x] 切片分析（按 query_type / difficulty 分组）
- [x] 评估结果保存到 JSON 文件（scripts/eval/results/）